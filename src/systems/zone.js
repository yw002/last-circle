// Blue zone (playzone) system — DISABLED (wave mode, no safe zone)

export function initZone() {
  // No-op: safe zone removed in wave survival mode
}

export function updateZone(delta) {
  // No-op: safe zone removed in wave survival mode
}
